fun main(){
   val count =  getCount("test")

}

fun getCount(str : String) : Int {
    var count : Int = 0
    val vocal : CharArray = charArrayOf('a','i','u','e','o')
    var char_change = str.toCharArray()

    for(E in char_change){
        if(E.equals(vocal)){

        }
    }
    return count
}